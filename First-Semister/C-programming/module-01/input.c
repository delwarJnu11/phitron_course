#include <stdio.h>

int main(){
    // int a;
    // float b;
    // char c;
    // scanf("%d %f %c", &a,&b,&c);
    // printf("%d %0.2f %c", a,b,c);
    // return 0;

    int a,b;
    scanf("%d%% %d%%", &a,&b);
    printf("%d%% %d%%", a,b);

    return 0;
}